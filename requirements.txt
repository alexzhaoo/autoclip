# Core dependencies
openai>=1.0.0
torch>=2.0.0
python-dotenv>=1.0.0
yt-dlp>=2023.1.0
tqdm>=4.64.0

# Video processing
opencv-python>=4.8.0
mediapipe>=0.10.0
numpy>=1.24.0
moviepy>=1.0.3
pysubs2>=1.6.0
pysrt>=1.1.2
decord>=0.6.0

# Audio processing
faster-whisper>=0.9.0
pydub>=0.25.0
elevenlabs>=0.2.0

# Text processing
nltk>=3.8.0

# AI/ML for B-roll generation
transformers>=4.30.0
accelerate>=0.20.0
pillow>=10.0.0
diffusers[torch]>=0.18.0

# Web requests
requests>=2.31.0

# Vast.ai SDK for cloud computing
vastai-sdk>=0.1.0

# Additional utilities
pathlib2>=2.3.0  # For older Python compatibility
dataclasses>=0.6; python_version<"3.7"  # Built-in for Python 3.7+
decord

# Note: Wan2.2 video generation system is not pip-installable
# It requires manual setup via git clone from https://github.com/Wan-Video/Wan2.2.git
# See run_on_vast.py for the complete installation process